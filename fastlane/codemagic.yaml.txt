workflows:
  ios-metadata-pipeline:
    name: Wispwood App Store Metadata Sync
    max_build_duration: 20
    instance_type: mac_mini_m1
    environment:
      groups:
        - app_store_credentials
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
    scripts:
      - name: Run Fastlane Metadata Deployment
        script: |
          bundle exec fastlane ios upload_meta_test